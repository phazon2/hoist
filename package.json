{
  "name": "hoist",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Every safety tool gates the irreversible action. This one reorders the plan around it.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "receipt": "node scripts/receipt.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
